name: CI Pipeline

on:
  pull_request:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: |
          xcodebuild build \
            -scheme RSSReader \
            -destination 'platform=macOS'

      - name: Test
        run: |
          # Skip UI tests in CI - they require a display context that GitHub Actions
          # runners don't fully support, causing hangs. UI tests should be run locally.
          xcodebuild test \
            -scheme RSSReader \
            -destination 'platform=macOS' \
            -skip-testing:RSSReaderUITests \
            -enableCodeCoverage YES \
            -resultBundlePath TestResults.xcresult
        timeout-minutes: 15

      - name: Coverage Report
        run: |
          xcrun xccov view --report --only-targets \
            TestResults.xcresult

  lint:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: SwiftLint
        run: swiftlint lint --strict
